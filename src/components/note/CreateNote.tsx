import React from "react";

interface CreateNoteRequest {
    title: string;
    content: string;
};

const CreateNote = () => {

    return (
        <div>

        </div>
    );
};

export default CreateNote;